services:
  - type: web
    name: puberphonia
    env: python
    buildCommand: apt-get update && apt-get install -y ffmpeg && pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: gunicorn voice_screener_api:app
